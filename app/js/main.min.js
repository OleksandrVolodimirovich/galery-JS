const slides=document.querySelectorAll(".slide");for(const s of slides)s.addEventListener("click",()=>{clearActiveClasses(),s.classList.add("active")});function clearActiveClasses(){slides.forEach(s=>{s.classList.remove("active")})}